The goal of this assignment is to get an understanding of how to commit and push changes in Git using the Git command line.
As well as how to merge two branches together, and to then merge the merged branches to the master branch.